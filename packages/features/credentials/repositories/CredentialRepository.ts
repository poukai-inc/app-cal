// Re-exported from @calcom/lib so that lower layers (e.g. @calcom/app-store) can
// consume this repository without importing from @calcom/features.
export { CredentialRepository } from "@calcom/lib/repositories/CredentialRepository";
